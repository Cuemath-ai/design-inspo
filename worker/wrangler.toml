name = "inspo-nudge"
main = "src/index.js"
compatibility_date = "2024-09-01"

# Public, non-secret config. The two secrets (SLACK_BOT_TOKEN, SLACK_SIGNING_SECRET)
# are set with `wrangler secret put` and never live in this file or git.
[vars]
INSPO_CHANNEL = "C0BA05V4C7M"
